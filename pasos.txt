Pruebas
puerto / command (Port, defien que necesita el caso de uso) -> Que es lo que necesito para realizar la logica solo la interfaz
use case (Use Case, Ejecuta lo del repository y que devuelve) -> Aplico reglas de negocio
interfaz HTTP (DTO, Define que acepta la API) -> Que es lo que me mandara el usuario
repository (Repository, implementa el metodo para la edicion) -> implementa la interfaz
Module (Module, cablea)
Controller (Controller, Expone la ruta y delega al use case) -> Se define la ruta