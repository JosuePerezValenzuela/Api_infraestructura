import { InventarioReporteRepository } from './inventario-reporte.repository';

describe('InventarioReporteRepository', () => {
  it('should be defined', () => {
    expect(new InventarioReporteRepository()).toBeDefined();
  });
});
